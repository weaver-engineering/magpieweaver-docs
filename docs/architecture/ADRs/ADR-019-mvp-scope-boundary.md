# ADR-019 — MVP Scope Boundary (Open — not yet finalized)

**Decision (draft, not approved):** This HLD documents the full intended
system, including capabilities well beyond a first release, deliberately —
so that MVP-stage design and implementation choices don't foreclose or
require expensive rework of later capabilities. The MVP itself is expected
to be substantially narrower than the full HLD (tentatively: MagpieEngine,
Weaver, and Scene Director only, single scene, no chronology, no cross-user
collaboration — see HLD §1a), but the exact boundary, and the specific
build-order commitments that follow from it, have not been finalized as an
approved decision.

**Status:** Open. Recorded now as a placeholder so the intent (document-the-
whole-system, build-a-narrow-slice-first) is traceable, without treating the
specifics in HLD §1a as locked. To be finalized in a future session.
